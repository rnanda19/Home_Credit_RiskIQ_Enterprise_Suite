# Sample outputs

These two images are **illustrative** — generated by actually executing
Mega Project 1's notebook 01 pipeline (real code, real SHAP computation,
real 4-model screening) end-to-end against the synthetic verification
fixture used to check this repo before delivery, not against the real
Kaggle Home Credit dataset. They exist to show what this project's real
output *looks like* (chart design, explainability depth, model-screening
rigor) — not to claim a result. Run the notebooks yourself against the
real, downloaded dataset (see the root README) to get your own real
numbers; this repository never ships real output derived from anyone's
actual data (see the zero-fabrication principle in the root README).

- `sample_shap_explainability.png` — from `01_credit_default_prediction.ipynb`'s
  SHAP feature-importance step.
- `sample_model_screening.png` — from the same notebook's 4-model screening
  stage (RandomForest / XGBoost / CatBoost / LightGBM).
